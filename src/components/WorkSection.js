import React, { Component } from 'react';
import { Section } from './Section';

export class WorkSection extends Component {
  constructor(props) {
    super(props);

    this.state = {};
  }

  render() {
    return <Section className="details-work"></Section>;
  }
}
